version: '3.9'
services:
  web-check:
    container_name: Web-Check
    build:
      context: .
      dockerfile: Dockerfile
    image: web-check:local
    ports:
      - "3000:3000"
    restart: unless-stopped